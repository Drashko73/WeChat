export const environment = {
  production: false,
  apiUrl: 'http://localhost:4200/api',
  // Default configurations that will be overridden by environment variables
  // when injected through Docker or other deployment methods
};