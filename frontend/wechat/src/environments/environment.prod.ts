export const environment = {
  production: true,
  apiUrl: 'http://api.wechat.com/api',
  // Production configurations that will be overridden by environment variables
  // when injected through Docker or other deployment methods
};
